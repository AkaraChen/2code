[package]
name = "acp-client"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["process", "io-util", "sync", "rt", "macros", "time"] }
tokio-stream = { version = "0.1", features = ["sync"] }
serde_json = "1"
futures = "0.3"
thiserror = "2"
tracing = "0.1"
