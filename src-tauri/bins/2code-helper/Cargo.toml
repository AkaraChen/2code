[package]
name = "twocode-helper"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "2code-helper"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = [ "derive" ] }
ureq = { version = "3", features = [ "json" ] }
model = { path = "../../crates/model" }
serde_json = "1"
